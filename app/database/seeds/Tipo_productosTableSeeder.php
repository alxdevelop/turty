<?php

class Tipo_productosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('tipo_productos')->truncate();

		$tipo_productos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('tipo_productos')->insert($tipo_productos);
	}

}
